import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ReporteVentas2Page } from './reporte-ventas2.page';

describe('ReporteVentas2Page', () => {
  let component: ReporteVentas2Page;
  let fixture: ComponentFixture<ReporteVentas2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ReporteVentas2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
